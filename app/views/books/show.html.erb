<div class="art">
  <div class="bl1">
    <h2 class="name_article"><%= @book.name %></h2>
    <span class="text-gray"><%=@book.created_at.strftime("%d %B, %Y") %></span>
    <br>
    <h6 class="text_article"><%= @book.content %></h6>
  </div>
  <div class="bl2">
    <% if @book.images.present? %>
      <%=image_tag(@book.images.first.url, {:style => "width: 550px;"}) %>
    <% end %>
  </div>
  <div class="bl3"> </div>
</div>
